import { DevelopersPage } from "../product-ui/Ecosystem";
export const metadata = { title: "Developers & API · Ganymede", description: "Read the verified USTX NAV from the public API or from X Layer, and embed a badge that verifies it in the browser." };
export default function Page() { return <DevelopersPage />; }
