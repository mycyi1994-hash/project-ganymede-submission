# Submission checkout

This is a review snapshot. Run npm ci and npm test without production credentials. Do not deploy to existing production Workers or run transactions during review. Preserve the private browser session identity boundary and the explicit trusted-edge header gate.
