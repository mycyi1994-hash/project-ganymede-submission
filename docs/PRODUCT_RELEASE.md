# Ganymede 제품 화면 통합 배포

2026-09-24 · `codex/product-redesign-1-3`

## 확정된 범위

제품의 실행 범위는 **USTX 데모 투자(데모 달러로 매수·환매, 바스켓 들여다보기, 펀드 개요), USTX 모델 NAV와 구성 공개·브라우저 검증, 발행 좌수의 X Layer 기록, 공개 NAV API와 임베드 배지, X Layer 메인넷 xStocks 읽기 전용 평가, 기존 CORE 테스트넷 지분 장부 읽기, 별도 Lab 모의 운용**이다. 실제 입출금·자산 취득·보관·지급·결제 토큰을 새로 만들거나 지정하지 않는다. 데모 주문은 실제 돈이 오가지 않으며, USTX 지분은 데모 장부에만 있고 체인에 발행되지 않는다.

USTX와 GMDCORE는 다른 대상이다. USTX는 모델 바스켓이다. 지갑 투자는 X Layer Testnet의 `GanymedeBasketFund`(USTX 지분 토큰)가 레지스트리의 최신 NAV로만 발행·환매하고, 대금은 가치 없는 `GanymedeDemoDollar`(dUSD)다. 지갑이 없으면 브라우저별 비공개 데모 계정(쿠키, 해시로만 저장)의 데모 달러로 투자하며 체인에는 아무것도 발행되지 않는다. 실제 돈은 어느 쪽에서도 움직이지 않는다. 배포된 `GanymedeFundShare`는 X Layer Testnet(1952)의 CORE 장부이며, 발행자 mint/burn은 입금·출금 증거가 아니다.

## 전체 화면

현재 운영 소스 `3211c6f` 기준이다. 모든 화면 위에 테스트넷 안내 한 줄이 있고, 헤더에 네트워크 표시와 "Connect OKX Wallet"이 있다. 메뉴는 Markets / Portfolio / Transparency다. 이전 배포별 변경은 아래 배포 절에 있다.

| 경로 | 완료한 동선 |
| --- | --- |
| `/` | Markets: USTX 최신 NAV("Priced by OKX OnchainOS")·펀드 규모·투자자 수·24시간 순유입·NAV 차트(24H/7D/All과 기간 수익률)·구성 비중·브라우저 검증 상태 → Invest |
| `/products/ustx` | 오른쪽 투자 패널: Wallet 탭(OKX Wallet 연결 → X Layer Testnet 전환 → dUSD 받기 → 승인·투자 또는 환매, 단계별 진행과 OKX 탐색기 링크)과 Demo balance 탭(데모 잔고로 매수·환매). 체결 후 "Added to your basket" 종목별 토큰 수량·금액, 펀드 개요(규모·발행 좌수·투자자·24시간 순유입·수익률·핵심 조건·USTX/dUSD 풀의 시장가격과 NAV 대비 괴리율·가격 오라클 OKX OnchainOS·마지막 NAV 기록과 OKX 탐색기), 보유 종목(전체 지분 기준 한 표), About USTX |
| `/products/ustx/transparency` | Verify(고객용 증명 페이지, 거래소의 준비금 증명 형식): 브라우저 자동 검증 결과, 가격 산정 3단계(OKX OnchainOS → X Layer 기록 → 브라우저 확인), 구성 종목과 OKX 가격, 최신 기록(발행 좌수·OKX 탐색기), 최근 기록, 확인 범위와 비범위 |
| `/portfolio` | 내 투자(총평가액·현금·투자원금·수익률, 보유 USTX, "Inside your USTX" 종목별 들여다보기, 최근 주문, 데모 잔고 초기화) → OKX Wallet 연결 또는 공개 주소 입력 → 그 지갑의 USTX(X Layer Testnet)와 종목별 들여다보기 → X Layer 메인넷 xStocks 6종 잔고(한 블록 기준) → 검증된 기록의 가격으로 평가·명세서 내려받기. 지갑 부분은 읽기 전용 |
| `/issuers` | 발행사용: 필요성, 4단계 작동 방식, 제공 기능, 플랜(Sandbox 무료, Issuer·Distribution은 문의), GitHub 문의. 푸터에서 연결 |
| `/developers` | 공개 NAV API 예시, 지갑·컨트랙트에서 투자하기(USTX·dUSD 주소와 viem 예시), 컨트랙트에서 NAV 읽기(Chainlink `AggregatorV3Interface` 가격 피드 주소와 Solidity 예시), X Layer에서 USTX 거래(풀·차익거래 주소, viem 예시, 실제 차익거래 거래), viem으로 X Layer 레지스트리 직접 읽기, 임베드 배지 코드와 실시간 미리보기, "Verify it yourself"(3가지 확인 결과, xStock 계약 6개 확인, 변조 실험 3종, 증거 파일 내려받기와 재확인 명령, 원문 문서), 사용한 OKX 스택 |
| `/embed/ustx` | 다른 사이트가 iframe으로 넣는 배지. 방문자 브라우저가 X Layer를 직접 읽고 문서 해시·NAV를 대조한 뒤 "Verified in your browser"를 표시 |
| `/api/v1/ustx` | 최신 USTX 기록(NAV·발행 좌수·지문·거래 해시)을 요청 시점에 X Layer에서 읽어 반환하고, USTX 지분 토큰, dUSD, 가격 피드, 풀·차익거래 주소를 함께 준다. CORS `*`, 30초 캐시, 읽기 전용 |
| `/api/demo/account`, `/api/demo/orders`, `/api/demo/reset`, `/api/demo/fund` | 데모 계정 조회(읽기 전용, 쿠키만 발급)·주문·초기화, 펀드 합계(지갑 USTX + 데모 잔고 지분, 투자자 수 포함)와 데모 잔고 주문의 24시간 유입(개별 주문은 공개하지 않음, 읽기 전용) |
| `/activity` | GMDCORE 테스트넷 장부의 공개 주소 잔액·지분 이전 로그 → 거래 상세. 메뉴에는 없음 |
| `/activity/[hash]` | 성공 receipt의 해당 CORE 계약 Transfer만 표시. 미존재·실패·다른 계약은 완료 처리하지 않음 |
| `/methodology`, `/limitations` | 공통 메뉴·본문 계층·문서 간 이동, 데이터 정책 |
| `/lab`, `/lab/strategies/[slug]` | 기존 KRW 모의 배분·내역·전략. USTX 및 테스트넷 장부와 명시적으로 구분. 메뉴에는 없음 |
| `/lab/verification` | 기존 가격 변경 실험 |
| 이전 `/proof`, `/?app=select`, `/?app=portfolio`, `/etfs/[slug]` | 각각 대응 화면으로 이동 |
| `/design-preview` | 개발 환경에서만 예시 주문·보유·진행 상태. 운영에서는 404 |

공통 메뉴는 Markets / Verify / Portfolio다. 모바일 하단 메뉴, 키보드 포커스, 메뉴 바깥 클릭·Escape 닫기, 짧은 선택/상태 모션과 움직임 줄이기 CSS를 유지했다. 조회 오류는 0 또는 성공으로 바꾸지 않는다. 계정이 바뀌면 이전 계정 잔액을 보여주지 않는다.

`/activity`의 장부 조회는 체인 ID, 배포 주소의 코드, decimals를 확인하고 같은 블록으로 잔액을 읽는다. 최근 2,000블록의 제한된 이력임을 화면에 표시하며 오래된 기록은 탐색기로 연결한다. 공개 RPC의 100블록 제한에 맞춰 최대 3개 동시 요청으로 나누어 읽는다. 계정 연결은 공개 주소 조회 권한 요청뿐이며 인증·서명·송금으로 사용하지 않는다. 공개 주소 편의 저장은 해당 탭의 sessionStorage만 쓴다.

## 통합 및 검수

- 기존 작업 `f4d67c1`에 현재 운영 소스인 Claude `df6c41e`를 통합했다. merge `2e81a09`. 거절 응답·금액 한도·공개 health 오류 정리 및 identity 신뢰 경계를 보존했다.
- 새 구현에서 `app/api`, `lib/engine`, `lib/xstocks`, `relayer`를 직접 변경하지 않았다.
- 타입검사, 새 운영 빌드, 전체 자동 검사 **89개 통과**. 마지막 공개 주소 조회 해제 변경 후 타입검사·운영 빌드와 관련 12개 검사도 다시 통과했다. 장부의 잘못된 체인·빈 계약·잘못된 값·누락 로그·실패 receipt·무관한 계약·큰 정수 정밀도, 운영 시안 차단과 경로 이동을 포함한다.
- lint: 오류 0. 기존 영역과 정적 SVG 이미지/기존 전체 이동 방식 등을 포함한 경고 10개. 이를 오류 없음과 별도로 기록한다.
- 실제 공개 RPC에서 08:55 UTC 기준 CORE 전체 발행량 0, 확인한 공개 주소 잔액 0, 최근 구간 이전 로그 없음. 브라우저에서도 공개 주소 입력과 다른 주소 전환, 잔액·활동 표시를 확인했다. 성공한 실제 지분 이전 사례는 관측하지 않았고 fixture 검사로 receipt 처리를 확인했다.
- 브라우저에서 실제 NAV 기록·SHA-256·계산의 일치를 확인했다. 09:00 UTC 기록과 구성 문서를 대조했다. 중간에 API 스냅샷보다 직접 RPC 기록이 먼저 갱신되자 미검증으로 남았으며 갱신 후 일치했다.
- 320/390 모바일의 계정·활동·문서, 1440 데스크톱과 이전 단계의 768/2560 핵심 화면을 점검했다. 실제 지갑 확장 프로그램의 계정 승인/전환, 사람 대상 사용성 조사와 모든 화면의 실제 400% 브라우저 확대를 검증 완료로 주장하지 않는다.

## 배포

대상은 기존 `ganymede-xlayer` Worker와 기존 D1이다. 새 DB 테이블이나 운영 자동화는 만들지 않는다. 9/25 지갑 투자 배포에서 테스트넷 계약 2개(dUSD, USTX)를 추가했다. 환경 변수·비밀 값과 5분 일정은 유지한다. 
- 현재 애플리케이션 소스: `3211c6f` (포트폴리오의 대출 담보·대출 표시, `main` `7e785de` 포함). 그 앞 배포는 아래 배포별 기록에 있다. 지갑 투자 배포의 소스는 `10dee7a`(`main` `405d04e` 포함)였다. 그 전 소스는 `cb921f5`(서비스형 화면 배치, `f970e23`·`f660dc3` 포함)이고, `f660dc3`은 고객용 증명 페이지·OKX 출처 표기·비중 막대 수정으로 `23d35d2`를 포함한다. `23d35d2`는 접근성 수정이고 `0c38037`을 포함한다.
- 현재 Worker 버전: `15ae856c-d433-4f10-a3c1-deb825c170b8`. 이전 버전은 `5f124eee-407a-4a12-bbb6-483f8c5642aa`, 그 전은 `de3e16c9-b6bd-4fe3-a025-7de5d3eb38a5`.
- 차익거래 키퍼 Worker `ganymede-arbitrage-keeper`: 현재 버전 `0f36c825-08fd-4aa7-ab82-9c0947343458`(소스 `f7d7c98`, 일정 `3-59/5 * * * *`). 이전 버전은 `5384d545-c0d2-4b56-ab67-e7153842da65`(비밀 값 등록)와 `3d2a8308-bd59-43f7-812a-3721f7ffe51a`(소스 `2218f1c`).
- 운영 주소: https://ganymede-xlayer.gana003.workers.dev
- 최초 통합 배포: `ff04920` → `bb33af8d-0c01-4c7e-ac20-0853854bfb74`. 직전 운영 버전 `b287ab71-66f9-45d0-8854-3ee28626f2c0`의 소스 `df6c41e`를 포함했다. 두 배포 모두 직전 활성 버전을 재확인하고 게시했다.
- 원격 저장소 브랜치: `codex/product-redesign-1-3`. 애플리케이션과 이 배포 기록을 푸시했다. 이 브랜치는 PR #4로 `main`에 통합됐다.
- 원격 바인딩 이름·종류 12개가 이전 버전과 일치했으며 기존 D1·비밀 값과 `*/5 * * * *` 일정을 유지했다. 강제 엔진 실행·거래·새 자동화는 수행하지 않았다.
- 09:06 UTC 운영 확인: 주요 9개 화면 200, 이전 4개 경로 307, 개발 시안 및 잘못된 거래 ID는 404. 공개 `/api/market`, `/api/health`, `/api/portfolio`, `/api/xstocks` 모두 200.
- 같은 확인에서 가격 평가 `2026-09-24T09:05:34.997Z`, 온체인 기록 `2026-09-24T09:05:34.000Z`, publication `confirmed`. 브라우저의 문서·SHA-256·NAV 대조도 일치했다. 샘플 확인 결과이며 연속 무장애 증명이 아니다.
- 운영 브라우저에서 Markets → 상품 → Transparency → Portfolio 공개 주소 조회 → Activity 동선을 확인했다. 마지막 배포에서 공개 주소 조회 해제와 계정 표시 초기화도 확인했으며 관측된 브라우저 오류 로그는 없었다.
- 필요 시 직전 호환 UI 버전 `bb33af8d-0c01-4c7e-ac20-0853854bfb74`로 되돌릴 수 있다. DB·온체인 기록을 삭제해 되돌리지 않는다.
- 2026-09-24 10:37 UTC 접근성 수정 배포: `f0ae49c` → `6e8ba126-42e0-4114-b355-77275a4bcd98`. 직전 버전은 `379aa941`이다.
  - 바뀐 점: Markets·USTX의 "/ USD" 단위 글자 대비를 4.36:1에서 6.08:1로 올렸다. Lab 검증 화면의 `aside` 두 개가 다른 랜드마크 안에 있던 중첩을 없앴다.
  - 배포 전: 활성 버전이 `379aa941`(`1bfed8e`)임을 재확인했다. 앱 코드 차이는 이 두 파일뿐이었다. 깨끗한 빌드와 `--keep-vars`로 배포했고 비밀 값 11개가 유지됐다.
  - 배포 후 운영 확인
    - 주요 10개 화면이 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다.
    - axe 검사에서 9개 화면 모두 위반 0건이었다.
    - 200%·400% 확대와 320·390px 폭에서 가로 넘침이 없었다.
    - 움직임 줄이기 설정에서 실행 중인 애니메이션이 0개였다.
    - 위조한 identity 헤더는 401로 거절됐다.
    - 5분 주기 발행이 계속 confirmed 상태였다.
- 2026-09-24 13:31 UTC 검증 중심 배포: `333ee0d` → `d95963d6-3a81-4c33-8f16-a9e9618371b0`. 직전 버전은 `6e8ba126`(`f0ae49c`)이다.
  - 바뀐 점
    - Markets·USTX에서 막힌 투자 요소를 없앴다. 두 화면이 브라우저 직접 검증 결과("Verified in your browser")를 보여 준다. 공통 메뉴는 Markets / Verify다. GMDCORE 장부 화면(`/portfolio`, `/activity`)과 Lab은 주소로만 들어간다.
    - 검증 화면에 확인 범위와 비범위, 가장 오래된 가격 나이, 원문 보관 범위(최근 12건)를 표시한다.
    - 세 가지 변조 실험(가격만 변경, 계산까지 맞춘 변경, NAV까지 같게 맞춘 변경)을 넣었다.
    - 증거 파일 내려받기와 `npm run verify:evidence`를 추가했다.
    - 공개 `/api/xstocks`의 체인 오류를 고정 문구로 바꿨다.
  - 배포 전: 활성 버전이 `6e8ba126`(`f0ae49c`)임을 확인했다. `333ee0d`가 그 소스를 포함한다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 97개가 통과했고 lint 오류는 0이다.
  - 배포 후 운영 확인
    - 주요 9개 화면이 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. 페이지 오류는 없었다.
    - Markets·USTX가 "Verified in your browser"를 표시했다. 검증 화면은 일치했다.
    - 변조 실험 결과는 순서대로 지문·계산·기록 판정이 실패·실패·일치, 실패·일치·실패, 실패·일치·일치였다.
    - 13:25:34 UTC 기록으로 내려받은 증거 파일이 `verify:evidence`에서 실제 거래 영수증(`0x74ac98cc…`)까지 PASS였다.
    - axe 검사에서 9개 화면 모두 위반 0건이었다. 200%·400% 확대와 320·390px 폭에서 가로 넘침이 없었다. 움직임 줄이기 설정에서 실행 중인 애니메이션은 0개였다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `6e8ba126`으로 되돌릴 수 있다.
- 2026-09-24 13:50 UTC Portfolio·차트 배포: `d055246` → `80d4b272-dfae-4fdf-b404-e15dddf54e15`. 직전 버전은 `d95963d6`(`333ee0d`)이다.
  - 바뀐 점
    - Portfolio가 연결한 지갑이나 입력한 공개 주소의 xStocks 6종 잔고를 X Layer 메인넷에서 한 블록 기준으로 읽는다. 방금 검증한 기록의 가격으로 평가하고, 평가 명세서와 USTX 비중 계산기를 제공한다. 읽기 전용이다.
    - 검증 화면이 문서의 종목 주소가 고정된 xStocks 계약인지 X Layer 메인넷에서 확인한다(코드·symbol·decimals).
    - 차트는 최대 7일의 NAV 기록을 쓴다. 과거 지점은 확정된 공시 거래의 영수증 NavPublished 이벤트에서 주기마다 24건씩 채운다. 이 작업은 공시 뒤에 따로 돌고, 실패해도 공시에는 경고만 남긴다.
    - 메뉴는 Markets / Verify / Portfolio다. GMDCORE 장부는 `/activity`에만 남는다.
  - 배포 전: 활성 버전이 `d95963d6`(`333ee0d`)임을 확인했다. `d055246`이 그 소스를 포함한다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 110개가 통과했고 lint 오류는 0이다. 실제 X Layer 영수증 두 건에서 복원한 NAV와 시각이 증거 파일과 같았다.
  - 배포 후 운영 확인
    - 주요 9개 화면이 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. 페이지 오류는 없었다.
    - xStock 계약 6개가 모두 일치했다.
    - Portfolio 계산기가 검증된 기록의 가격과 비중을 썼다.
    - 변조 실험 세 가지 결과가 의도대로였다. 13:50:17 UTC 증거 파일은 실제 거래 영수증(`0x1e2467a8…`)까지 PASS였다.
    - axe 검사에서 Portfolio를 포함한 화면 위반이 0건이었다. 확대와 좁은 폭에서 가로 넘침이 없었다.
    - 배포 후 첫 주기(13:55 UTC)에 NAV 기록 24개 지점이 생겼고, 이후 주기마다 과거로 채워진다.
  - 필요 시 `d95963d6`으로 되돌릴 수 있다. 되돌려도 저장된 NAV 기록 상태는 남으며 공시에 영향이 없다.
- 2026-09-24 14:16 UTC 차트 기록 개수 수정 배포: `a65d6d7` → `cabd72ed-5134-4df3-aa21-534d16a74449`. 직전 버전은 `80d4b272`(`d055246`)이다.
  - 바뀐 점: 공개 API는 차트용 NAV 기록을 300개 안팎으로 솎아 보낸다. 차트 문구 "N published records"가 그린 점의 개수를 세고 있어, 공시가 300건을 넘으면 실제보다 적게 표시될 수 있었다. API가 솎기 전 개수(`seriesCount`)도 보내고, 문구는 그 개수를 쓴다. 그린 점이 더 적으면 "(M shown)"을 덧붙인다.
  - 배포 전: 활성 버전이 `80d4b272`(`d055246`)임을 확인했다. `a65d6d7`이 그 소스와 현재 `main`을 포함한다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 111개가 통과했고 lint 오류는 0이다. 로컬 화면에서 솎인 기록 450건을 넣어 "450 published records … (237 shown)"로 표시되는 것을 확인했다.
  - 배포 후 운영 확인
    - 주요 9개 화면이 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. 페이지 오류는 없었다.
    - Markets·USTX·Portfolio가 "Verified in your browser"를 표시했다. xStock 계약 6개가 모두 일치했다.
    - 차트 문구와 API `seriesCount`가 같은 개수(124건, 24 Sept 03:51 UTC부터)를 보였다. 이 시점에는 과거 기록 채움이 진행 중이었다.
    - 변조 실험 세 가지 결과가 의도대로였다. 14:15:17 UTC 증거 파일은 실제 거래 영수증(`0xa1648ba6…`)까지 PASS였다.
    - axe 검사에서 9개 화면 모두 위반 0건이었다. 200%·400% 확대와 320·390px 폭에서 가로 넘침이 없었다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `80d4b272`로 되돌릴 수 있다. API의 새 필드만 없어지고 차트는 그린 점의 개수로 돌아간다.

- 2026-09-24 18:07 UTC 데모 투자 배포: `1327a6a` → `7be4c726-55a8-4685-badc-57f94587849e`. 직전 버전은 `cabd72ed`(`a65d6d7`)이다.
  - 바뀐 점
    - USTX를 데모 달러로 사고 환매한다. 브라우저마다 $10,000로 시작하는 비공개 데모 계정(쿠키, 해시로만 저장)이 있고, 주문은 X Layer에 기록된 최신 NAV(1시간 이내)로 즉시 체결된다. 최소 주문 $10, 수수료 없음.
    - 주문은 한 번의 D1 트랜잭션으로 처리한다. 같은 주문 ID의 재시도는 첫 체결을 다시 돌려준다. 전체 계정 합산 하루 주문 한도(2,000건)가 NAV 공시에 필요한 DB 쓰기 예산을 지킨다.
    - Portfolio에 데모 계정(총평가액·수익률·보유·최근 주문·초기화)을 추가했다. 메뉴 순서는 Markets / Portfolio / Verify다.
    - 가격 제공자 대기 시간이 1분 안에 끝나면 대기로 보지 않아, 429 뒤 공시 공백이 한 주기 줄었다.
  - 배포 전: D1에 `drizzle/0001_demo_ledger.sql`(표 3개 추가, 기존 표 변경 없음)을 적용했다. 활성 버전이 `cabd72ed`(`a65d6d7`)임을 확인했다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 118개가 통과했고 lint 오류는 0이다.
  - 배포 후 운영 확인: 주요 화면 200, 이전 경로 이동, 운영 브라우저에서 계정 열기 → 매수 → 포트폴리오 반영 → 환매 → 초기화를 확인했다.
- 2026-09-24 18:32 UTC 펀드 상품화 배포: `0c38037` → `8fa91c44-04f8-40d5-9bc0-b564dd4e7be3`. 직전 버전은 `7be4c726`(`1327a6a`)이다.
  - 바뀐 점
    - 체결 화면이 이번 주문으로 바스켓에 담긴(환매는 빠진) 종목별 토큰 수량과 금액을 보여 준다. Portfolio는 보유 지분을 6개 xStock으로 들여다본다(좌수 × 좌당 고정 토큰 수량, 기록된 가격).
    - USTX 화면에 펀드 개요를 넣었다. 펀드 규모는 X Layer에 기록된 발행 좌수 × 기록된 NAV다. 투자자 수, 출시(23 Sep 2026, $100.00) 후 수익률, 핵심 조건, 전체 지분의 종목별 보유, 최근 익명 주문을 보여 준다. Markets 카드에도 펀드 규모·투자자·수익률을 표시한다.
    - 5분마다 올리는 NAV 기록에 데모 장부의 발행 좌수를 함께 올린다. 재시도는 처음 저장한 좌수를 그대로 보낸다. 데모 표를 읽지 못하면 0을 올리고 경고를 남긴다.
    - 공개 `GET /api/v1/ustx`(CORS 개방), 임베드 배지 `/embed/ustx`, 발행사 페이지 `/issuers`, 개발자 페이지 `/developers`, Markets의 "Built on X Layer and OKX" 구역을 추가했다.
    - 문구를 완성된 펀드 상품 기준으로 바꾸되 데모·테스트넷 표시는 유지했다. 계정·펀드 금액은 센트 단위로 반올림한다($1,000 주문이 $999.99로 보이지 않게).
  - 배포 전: 활성 버전이 `7be4c726`(`1327a6a`)임을 확인했다. `0c38037`이 그 소스와 현재 `main`을 포함한다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 125개가 통과했고 lint 오류는 0이다.
  - 배포 후 운영 확인
    - 주요 11개 화면과 공개 API 3개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. `/api/v1/ustx`가 `access-control-allow-origin: *`을 보냈다.
    - 배포 뒤 첫 기록(18:35:17 UTC, 거래 `0x6d157072…`)의 `sharesOutstandingMicros`가 데모 장부 합계 `10034991`과 같았다.
    - axe 검사에서 `/developers`의 가로 스크롤 코드 블록 2개와 `/embed/ustx`의 h1 누락이 나와 다음 배포에서 고쳤다. 나머지 화면은 위반 0건, 확대와 좁은 폭에서 가로 넘침이 없었다.
- 2026-09-24 18:38 UTC 접근성 수정 배포: `23d35d2` → `bebf23c3-427f-4bdf-b521-6ead2b86b7d6`. 직전 버전은 `8fa91c44`(`0c38037`)이다.
  - 바뀐 점: 개발자 페이지 코드 블록에 키보드 포커스(영역 이름 포함)를 주고, 임베드 배지 상품명을 h1로 바꿨다.
  - 배포 전: 활성 버전이 `8fa91c44`(`0c38037`)임을 확인했다. 깨끗한 빌드와 `--keep-vars`로 배포했다. 전체 검사 125개가 통과했고 lint 오류는 0이다.
  - 배포 후 운영 확인
    - `/developers`·`/embed/ustx`를 포함해 11개 화면 axe 위반 0건. 임베드 배지는 방문자 브라우저에서 "Verified in your browser"까지 확인했다.
    - 운영 브라우저에서 USTX $250 데모 매수 → "Added to your basket" 6종목 표시 → 펀드 최근 거래 반영 → Portfolio 총평가액 $10,000.00·"Inside your USTX"를 확인했다. 페이지 오류는 없었다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `7be4c726`으로 되돌릴 수 있다. 되돌려도 데모 장부와 기록된 NAV는 남는다. 이후 기록은 발행 좌수 0으로 올라간다.

- 2026-09-25 01:0x UTC 고객용 증명 페이지 배포: `f660dc3` → `26694863-3284-4e16-97a9-ad4862b2ea44`. 직전 버전은 `bebf23c3`(`23d35d2`)이다.
  - 바뀐 점
    - 구성 종목 비중 막대: 예전 화면용 전역 규칙(`button { min-height: 46px }`) 때문에 29px 막대 안 버튼이 46px로 커졌고, 등장 애니메이션이 남긴 `clip-path`가 그 아래를 잘라 글자가 막대 바닥에 붙고 선택 테두리가 사라졌다. 버튼이 막대 높이를 채우게 하고, 애니메이션이 끝나면 `clip-path`가 남지 않게 했다.
    - OKX 출처 표기: NAV 옆·차트 설명·투자 패널·펀드 개요·푸터에 "Priced by OKX OnchainOS", 가격 열은 "OKX price", 거래·레지스트리 링크는 "OKX Explorer", 지갑 버튼은 "Connect OKX Wallet".
    - Verify 화면을 거래소 준비금 증명 페이지 형식의 고객용 화면으로 바꿨다. 3가지 확인 결과, 변조 실험, 증거 파일, 원문 문서는 `/developers`의 "Verify it yourself"로 옮겼다.
    - 펀드 개요의 개별 주문 목록을 "Net flows, 24h"(유입·유출·주문 수 합계)로 바꿨다. `/api/demo/fund`도 합계만 반환한다.
    - 고객 화면 문구: NAV per share, Proof of NAV, Demo fund. "model share", "published record" 같은 표현을 없앴다.
  - 운영 데이터: 사용자 승인에 따라 운영 확인용으로 넣었던 $250 데모 주문과 그 계정을 D1에서 지웠다(주문 1행, 계정 1행). 01:00 UTC 기록부터 발행 좌수 70.195063이 X Layer에 올라갔다.
  - 배포 전: 활성 버전이 `bebf23c3`(`23d35d2`)임을 확인했다. `f660dc3`이 현재 `main`(`38cbdc1`)을 포함한다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 126개가 통과했고 lint 오류는 0이다.
  - 배포 후 운영 확인
    - 주요 11개 화면과 공개 API 3개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다.
    - 1536px·125% 화면에서 비중 막대 버튼 높이가 29px였고 `clip-path`가 남지 않았다.
    - Transparency가 "NAV verified on X Layer"와 발행 좌수 70.19를 표시했다. 개발자 페이지의 세 가지 확인도 모두 통과했다.
    - axe 검사에서 6개 화면 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `bebf23c3`로 되돌릴 수 있다. 그 버전의 펀드 개요는 `recent` 필드를 기대하므로 되돌릴 때는 API도 함께 되돌아간다(같은 Worker).

- 2026-09-25 서비스형 화면 배치 배포: `f970e23` → `837c8a3b-38a4-4544-8ba7-4d6a7899fa05`, 이어서 `cb921f5` → `3a46801e-e6f9-47e0-af1c-8477f24bef71`. 직전 버전은 `26694863`(`f660dc3`)이다.
  - 바뀐 점
    - 헤더: 테스트넷 안내 한 줄(헤더 안), 네트워크 표시 "X Layer Testnet", "Connect OKX Wallet" 버튼(지갑이 없으면 Portfolio의 지갑 절로 안내). 메뉴 Markets / Portfolio / Transparency.
    - Markets: 발표용 구역("What you can do" 카드, "Built on X Layer and OKX", 발행사·개발자 배너)을 없앴다. 카드에는 펀드 규모·투자자·24시간 순유입이 있고, 차트는 24H/7D/All 기간과 기간 수익률을 보여 준다.
    - USTX: 오른쪽에는 투자 패널만 둔다. 가격 오라클과 마지막 NAV 기록(OKX 탐색기)을 펀드 개요로 옮기고, 보유 종목은 전체 지분 기준 한 표로 합쳤다. "Terms & approach"는 "About USTX"로 바꿨다.
    - 투자 패널과 Portfolio의 반복 경고를 "Demo balance" 표현으로 바꾸고, Portfolio의 비중 계산기를 없앴다.
    - 발행사 페이지의 예정 요금과 로드맵을 "Plans"(Sandbox 무료, 나머지 문의)로 바꿨다. 제품 문서에서 Lab 링크를 없앴다. 푸터: Transparency · Methodology · Risks · Docs · For issuers.
    - 첫 배포 뒤 axe가 헤더 밖 안내 줄을 랜드마크 누락(moderate)으로 잡아, 두 번째 배포에서 안내 줄을 헤더 안으로 옮겼다.
  - 배포 전: 두 배포 모두 활성 버전을 확인했고, 소스가 현재 `main`(`65e0625`)을 포함한다. 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 전체 검사 126개가 통과했고 lint 오류는 0이다.
  - 배포 후 운영 확인
    - 주요 11개 화면과 공개 API 3개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다.
    - 데스크톱(1536px·125%)과 모바일(390px)에서 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - USTX 보유 종목 표 6행과 가격 오라클 표시, Transparency "NAV verified on X Layer", 비중 막대 버튼 29px를 확인했다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `26694863`으로 되돌릴 수 있다. API와 데이터는 바뀌지 않았다.

실자금 제품으로 확장하려면 결제·보관·취득·지급 구조와 계정 인증·견적·거래 상태 계약이 별도 구현되어야 한다. 데모 투자 화면은 데모 달러임을 계속 표시하며, 실제 돈을 받는 경로가 있는 것처럼 보이지 않게 유지한다.

- 2026-09-25 지갑 투자 배포: `10dee7a` → `f760cd9e-bf95-47a0-8d4d-4a0fa4886c21`. 직전 버전은 `3a46801e`(`cb921f5`)이다. 사용자가 규칙 변경(테스트넷 데모 달러에 한해 투자·환매를 연다)과 운영 배포를 승인했다.
  - 컨트랙트(X Layer Testnet, 관리자 키로 배포, `onchain/deployments/xlayer-testnet.json`)
    - `GanymedeDemoDollar`(dUSD, 가치 없음) `0xf07535080f74e8b0f571e58dfa600f47e72ea9bf`: 누구나 하루 한 번 10,000개를 받는다. 발행 권한은 펀드 계약에만 있다(`setMinter` 거래 `0x25ca194461481a5ab190772e742754e914558a6db505e6545ab7147eca752516`).
    - `GanymedeBasketFund`(USTX) `0x77eaeba1366bde7818da12d3cbdbea0a2ee97596`: 레지스트리의 `us-tech-x` 최신 NAV(1시간 이내)로만 발행·환매한다. 최소 $10, 양방향 내림, 최소 수령량, 보유 자산 없음, 보유자 수 온체인 집계, 관리자는 일시 정지만 가능하다.
    - 관리자 지갑 테스트넷 OKB 사용량은 약 0.00204다(테스트 지갑 가스비 0.002 포함).
  - 바뀐 점
    - 투자 패널에 Wallet / Demo balance 탭을 추가했다. 브라우저에 지갑이 있으면 Wallet으로 연다. 읽기는 페이지가 본 가장 최근 블록에 고정하고, 주문은 지갑을 열기 전에 모의 실행한다.
    - NAV 기록의 발행 좌수, 펀드 규모와 투자자 수에 지갑 USTX를 더한다. 5분 주기가 마지막으로 읽은 값을 `engine_state`의 `xstocks:wallet-shares`에 두고, 체인을 못 읽으면 그 값을 쓰며 경고를 남긴다.
    - Portfolio에 지갑의 USTX를 표시한다. Transparency, 개발자, 발행사 페이지와 공개 API에 지분 토큰을 표시한다.
    - AGENTS.md의 범위 규칙을 승인된 대로 바꿨다.
  - 배포 전
    - 활성 버전이 `3a46801e`(`cb921f5`)임을 확인했다. 소스가 현재 `main`(`405d04e`)을 포함한다.
    - 앱 검사 134개, 컨트랙트 검사 29개가 통과했고 lint 오류는 0이다(경고 10개는 main과 같다).
    - 로컬 빌드에서 테스트 지갑을 브라우저에 주입해 실제 X Layer Testnet 거래로 연결부터 전량 환매까지 확인했다. 흐름은 연결 → 네트워크 전환 → 요청 거절 → 승인 → 투자 → Portfolio → 전량 환매였다.
    - 테스트 지분은 모두 환매되어 발행량과 투자자 수가 0으로 돌아왔다.
    - 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다.
  - 배포 후 운영 확인
    - 주요 11개 화면과 공개 API 3개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다.
    - `/api/demo/fund`가 지갑 합계를 체인에서 읽었다(블록 41,847,877, 0주·0명). `/api/v1/ustx`에 지분 토큰이 나온다.
    - 배포 뒤 첫 기록(03:26 UTC)이 경고 없이 확정됐다. 발행 좌수는 70.195063(데모 잔고 지분 + 지갑 0)이고, `xstocks:wallet-shares`가 블록 41,847,933에서 저장됐다.
    - 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다. 운영에서는 거래 없이 지갑 연결·네트워크 전환·잔고 표시까지만 확인했다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `3a46801e`로 되돌릴 수 있다. 그 버전은 지갑 탭 없이 동작하고 NAV 기록에 데모 잔고 지분만 싣는다. 이미 발행된 지갑의 USTX는 체인에 남고, 컨트랙트에서 직접 환매할 수 있다.
- 2026-09-25 가격 피드 배포: `e36c7a3` → `6f647815-4665-40fd-b098-9146546f512d`. 직전 버전은 `f760cd9e`(`10dee7a`)이다. 사용자가 피드 배포와 운영 배포를 승인했다.
  - 컨트랙트(X Layer Testnet, 관리자 키로 배포, `onchain/deployments/xlayer-testnet.json`)
    - `GanymedeNavFeed`("USTX / USD", 소수 8자리) `0x292c56c5290cc7b73e3ee33c2c2688eb3e04c3c8`, 생성 거래 `0x816e42ff26474f01334a3c7ca16659d70e337a331b0a507d80102cf11e2a6ed3`. 레지스트리의 `us-tech-x` 최신 NAV를 Chainlink `AggregatorV3Interface`로 돌려준다. 라운드 ID와 `updatedAt`은 기록의 효력 시각이다. 소유자와 설정이 없다.
    - OKX 탐색기 소스 검증을 마쳤고 Sourcify에서 생성·런타임 바이트코드가 완전 일치했다. 기존 4개 컨트랙트도 Sourcify에서 완전 일치를 받았다.
    - 관리자 지갑 테스트넷 OKB 사용량은 약 0.00001이다.
    - 대출 시장(`GanymedeLendingMarket`)은 작성·테스트만 했고 배포하지 않았다.
  - 바뀐 점
    - `/developers`에 "Read the NAV from a contract"(피드 설명, 컴파일되는 Solidity 예시, 탐색기 링크)를 넣었다. OKX 스택 목록의 X Layer Testnet 항목에 피드를 넣었다.
    - `/api/v1/ustx`가 `feed`(주소, 인터페이스, 소수 자릿수, 탐색기 링크)를 함께 준다.
    - 피드 주소는 `FUND_DEPLOYMENT.feed`에 고정했고, 배포 기록과 대조하는 테스트를 추가했다.
  - 배포 전
    - 활성 버전이 `f760cd9e`(`10dee7a`)임을 확인했다. 소스가 현재 `main`(`55cb87f`)을 포함한다.
    - 앱 검사 134개, 컨트랙트 검사 50개가 통과했고 lint 오류는 0이다(경고 10개는 main과 같다).
    - 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다.
  - 배포 후 운영 확인
    - 주요 11개 화면과 공개 API 2개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다.
    - `/api/v1/ustx`의 `feed`와 `/developers`의 피드 절을 확인했다.
    - 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `f760cd9e`로 되돌릴 수 있다. 피드 컨트랙트는 체인에 남아 계속 동작한다.
- 2026-09-25 USTX 시장 배포: `1e2693b` → `f460620d-8c77-4584-81d0-92abbf44e748`. 직전 버전은 `6f647815`(`e36c7a3`)이다. 사용자가 풀 배포를 승인했고, 운영 배포는 묻지 않고 하라고 했다.
  - 컨트랙트(X Layer Testnet, 관리자 키로 배포, `onchain/deployments/xlayer-testnet.json`)
    - `GanymedeUstxPool`(USTX/dUSD 상수곱 풀, 수수료 0.3%) `0x286f5e7ffdbc30db12665d7a3854217d7cd05cc1`. 관리자 지갑이 데모 달러 10,000을 받아 $5,000을 NAV로 투자하고, 받은 50.1174 USTX와 같은 가치의 데모 달러로 유동성을 넣었다(거래 `0x817304cf7026e580e2f231ee01f4f15d8f29eca05fd807c6452b28382d7d30a0`). 풀 가격 $99.765748, NAV $99.765749로 열렸다.
    - `GanymedeNavArbitrage` `0xaeba15aa92d6f3109e2b992f18933e1abe2fa3d9`. 풀이 NAV보다 싸면 풀에서 사서 펀드에 환매하고, 비싸면 펀드에서 사서 풀에 판다. 거래 하나로 하며, 호출자가 넣은 금액 이상을 돌려받지 못하면 되돌린다.
    - 두 컨트랙트 모두 Sourcify에서 생성·런타임 바이트코드가 완전 일치했고, 사용자가 올린 소스로 OKX 탐색기 검증도 마쳤다.
    - 테스트 지갑으로 실제 차익거래를 했다. 판매자가 5.01174 USTX를 풀에 팔아 가격이 NAV보다 17.3% 낮아졌고, `buyAndRedeem`이 $447.82를 넣어 $491.80을 돌려받았다(거래 `0x3c604c934a1ef7576a173e0b513c419ad89376f1c6bea17464f8c5afbb9f6c2e`). 풀은 NAV보다 0.27% 낮은 수준, 즉 수수료 안으로 돌아왔다.
  - 바뀐 점
    - USTX 펀드 개요에 "Market price"(풀 가격, NAV 대비 괴리율, 탐색기 링크)를 넣었다. 한 블록에서 풀 잔고와 NAV를 함께 읽는다.
    - `/developers`에 "Trade USTX on X Layer"(설명, viem 예시, 풀과 실제 차익거래 링크)를 넣었다. `/api/v1/ustx`가 `market`(풀·차익거래 주소)을 준다.
    - 풀도 USTX 보유자이므로 투자자 설명을 "wallets"에서 "holders"로 바꿨다. 펀드 규모와 NAV 기록의 발행 좌수에는 풀이 가진 USTX가 들어간다.
  - 배포 전
    - 활성 버전이 `6f647815`(`e36c7a3`)임을 확인했다. 소스가 현재 `main`(`4545c2c`)을 포함한다.
    - 앱 검사 135개, 컨트랙트 검사 60개가 통과했고 lint 오류는 0이다(경고 10개는 main과 같다).
    - 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다.
  - 배포 후 운영 확인
    - 주요 11개 화면과 공개 API 2개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다.
    - USTX 화면의 시장가격 줄이 "$99.49 · 0.27% below NAV · USTX/dUSD pool"로 나왔다. `/developers`의 시장 절과 `/api/v1/ustx`의 `market`을 확인했다.
    - 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - 배포 뒤 첫 기록(05:16 UTC)이 확정됐고, 발행 좌수 130.419139에 풀의 USTX가 들어 있다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `6f647815`로 되돌릴 수 있다. 풀과 차익거래 컨트랙트는 체인에 남아 계속 동작한다.
- 2026-09-25 차익거래 키퍼 배포: 새 Worker `ganymede-arbitrage-keeper`, `2218f1c` → `3d2a8308-bd59-43f7-812a-3721f7ffe51a`, 키 등록 뒤 `5384d545-c0d2-4b56-ab67-e7153842da65`. 운영 배포는 묻지 않고 하라는 사용자 지시에 따랐다.
  - 키퍼는 자기 키(`KEEPER_PRIVATE_KEY`, Worker 비밀 값)로 5분마다 `GanymedeNavArbitrage.quote()`를 읽고, 풀과 NAV의 괴리를 좁히는 거래를 보낸다. 어떤 컨트랙트에도 권한이 없다. 키는 새로 만들었고 저장소와 로그에 없다.
  - 키퍼 주소 `0xccF372068496d9beF0f7cF83d697183D358dEc1B`. 관리자 지갑이 테스트넷 OKB 0.01을 가스용으로 보냈다(거래 `0x6e2761c2ba10a6965b7354ee0719ff3fe5489a0455d9b69639f62c1bb1d93637`). 거래 6건에 약 0.000015 OKB를 썼다. 데모 달러는 키퍼가 직접 받는다.
  - 첫 실행(06:05 UTC): 데모 달러 받기와 승인은 성공했고, `buyAndRedeem(1834955, 288)`은 `Unprofitable(1834955, 1834943)`로 되돌려졌다(거래 `0x56437e76bde092960bd1967526922b21e2176b1d2d9fba6434a655fd62977941`). 키퍼가 견적을 낸 뒤 앱의 06:05 NAV 기록(99.865711 → 99.833714)이 먼저 체인에 올라가, 이익 $0.0006짜리 $1.83 거래가 손해가 됐다. 컨트랙트의 손실 방지 조건대로 되돌려졌고 가스만 썼다. 06:10과 06:15에는 $1.03, $1.15 거래가 성공했다.
- 2026-09-25 키퍼 설명 배포: `205ebe1` → `09816f8c-cbcd-458d-8a19-2e1bf14c28ba`. 직전 버전은 `f460620d`(`1e2693b`)이다. `/developers`의 시장 절과 `/api/v1/ustx`의 `market.rule`에 키퍼를 넣었다. 소스가 `main`(`8373527`)을 포함한다. 주요 화면·공개 API 200, 이전 4개 경로 이동, axe 위반 0건을 확인했다.
- 2026-09-25 키퍼 수정 배포: 키퍼 `f7d7c98` → `0f36c825-08fd-4aa7-ab82-9c0947343458`(직전 `5384d545`), 앱 `f7d7c98` → `e7c29424-d4f4-4dfe-8815-824f3747bc1f`(직전 `09816f8c`, `205ebe1`).
  - 바뀐 점
    - 일정을 `3-59/5 * * * *`로 옮겼다. 최근 2시간의 NAV 기록은 5분 경계 뒤 8~120초에 올라왔으므로, 경계 3분 뒤에 실행하면 그 경계의 기록이 이미 올라와 있고 다음 기록까지 2분이 남는다.
    - 거래 전에 같은 거래를 호출로 먼저 실행한다(`simulateContract`). 되돌려질 거래는 보내지 않고, 되돌림 이유를 `Unprofitable(…)`처럼 컨트랙트 오류 이름으로 남긴다.
    - 괴리를 좁혀 1센트(10,000 마이크로 데모 달러) 이상 벌 때만 거래하고, 호출로 확인한 이익의 절반을 최소 이익으로 건다. 데모 달러 받기나 승인 뒤에는 다시 견적을 낸다.
    - `/developers`와 `/api/v1/ustx`의 키퍼 문구를 "5분마다 확인하고, 괴리를 좁혀 1센트 이상 벌 때 거래한다"로 바꿨다.
  - 배포 전
    - 앱 활성 버전이 `09816f8c`(`205ebe1`), 키퍼 활성 버전이 `5384d545`임을 확인했다. 소스가 현재 `main`(`8373527`)을 포함한다.
    - 앱 검사 135개, relayer 검사 28개(키퍼 12개)가 통과했고 lint 오류는 0이다(경고 10개는 main과 같다). 키퍼 키로 쓰기 없이 실제 체인에 모의 실행해, 호출 결과와 되돌림 이유(`Unprofitable(500000000, 454840736)`)를 받는 것을 확인했다.
    - 앱은 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다. 키퍼의 비밀 값은 그대로 남았다.
  - 배포 후 운영 확인
    - 주요 12개 화면과 공개 API 4개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. `/developers`와 `/api/v1/ustx`에 새 키퍼 문구가 나온다.
    - 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - 키퍼 실제 동작: 테스트 지갑이 $150을 NAV로 투자해 받은 1.501362 USTX를 풀에 팔아 풀을 NAV보다 6.02% 낮췄다(거래 `0xb467547f88971d511199fbaf1e21d68c264bccfe3be6d5b9d60cf2aa1e727ff4`). 06:23 UTC 키퍼가 사람 개입 없이 `buyAndRedeem(145924920, 2187752)`로 $145.92를 넣어 $150.30을 돌려받았고(이익 $4.38, 거래 `0xbec5c89a1546e65c1f03a4131c85c1ef50e1ac9e3f4e6e09f929b33f7c33d26f`), 풀은 NAV보다 0.29% 낮은 수준, 즉 수수료 안으로 돌아왔다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 앱은 `09816f8c`, 키퍼는 `5384d545`로 되돌릴 수 있다. 키퍼를 멈추려면 Worker의 일정을 지우면 되고, 컨트랙트와 풀은 그대로 동작한다.
- 2026-09-25 키퍼 링크 배포: `1e0465f` → `b203515c-1232-4301-a9f6-6db509f13575`. 직전 버전은 `e7c29424`(`f7d7c98`)이다.
  - `/developers`의 시장 절에 키퍼의 실제 거래("A keeper trade that closed a 6% gap")와 키퍼 지갑 링크를 넣었다. `/api/v1/ustx`가 `market.keeper`로 키퍼 지갑 주소를 준다. 주소는 `FUND_DEPLOYMENT.keeper`에 있다.
  - 배포 전: 활성 버전 `e7c29424` 확인, 소스가 `main`(`8373527`)을 포함, 앱 검사 135개 통과, lint 오류 0, 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`.
  - 배포 후: 주요 12개 화면과 공개 API 4개 200, 이전 4개 경로 307, 새 링크 두 개와 `market.keeper` 확인. 데스크톱과 모바일에서 링크가 한 줄씩 넘침 없이 배치된다. 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다. 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `e7c29424`로 되돌릴 수 있다.
- 2026-09-25 대출 시장 배포(일시정지): Worker 변경 없음. 사용자가 "다음 진행해"로 앞서 안내한 대출 시장 배포를 진행하게 했고, 배포 뒤 "대출 배포 승인, 기록 올려"로 명시 승인했다. 활성화(`unpause`)는 승인받지 않았고 하지 않았다.
  - `GanymedeLendingMarket` `0xae2f54ae3d0370295de18510d56de92afb8843c7`, 생성 거래 `0xb771847aebe5f893eec25e97f99c69cc55f0f85fcaa71217facab2bd7df26278`(가스 1,742,871). 관리자 키로 배포했고 `deployments/xlayer-testnet.json`에 생성 거래와 함께 기록했다. 대출 자산은 dUSD, 담보는 USTX(펀드의 `currentNav()`로 평가), 관리자는 기존 관리자 지갑이다.
  - 일시정지 상태라 공급·담보 예치·대출이 모두 막혀 있다. 앱에는 넣지 않았다.
  - 배포 전
    - 온체인 타입검사와 컨트랙트 검사 60개가 통과했다. 배포 스크립트는 풀 배포와 같이 nonce·가스 한도를 직접 정하고 영수증을 기다리도록 고쳤다(`eth_estimateGas` 1,757,871, 한도 2,300,000).
    - 실제 체인을 복제한 로컬 포크(체인 1952)에서, 실제 자금이 없는 키로 배포 스크립트를 먼저 실행해 연결 확인과 일시정지 상태를 확인했다. 실제 배포 기록 파일은 리허설 뒤 원본으로 되돌렸다.
    - `npm run fork:lending`으로 실제 컨트랙트를 복제한 메모리 포크에서 공급 $5,000, NAV로 산 USTX 담보, $950 대출, NAV 30% 하락, 청산(보너스 8%), 압류 USTX의 펀드 환매, 상환·인출까지 한 바퀴를 확인했다. 아무것도 전송하지 않았다.
  - 배포 후
    - 체인에서 `dollar`, `fund`, `administrator`, `paused = true`를 다시 읽어 확인했다.
    - Sourcify에서 생성·런타임 바이트코드가 완전 일치했다. OKX 탐색기에는 사용자가 소스를 올렸고(`onchain/deployments/verification/xlayer-testnet/GanymedeLendingMarket.*`, 캡차 때문에 사용자 몫), 탐색기의 Contracts 탭에서 "Contract source code verified"를 확인했다. 이로써 X Layer Testnet 컨트랙트 8개 모두 OKX 탐색기와 Sourcify에서 검증됐다.
- 2026-09-25 최적 가격 주문 배포: `73d543f` → `5d7eebbc-b2c4-49fa-b7c1-23fe78270fb8`. 직전 버전은 `b203515c`(`1e0465f`)이다.
  - 바뀐 점
    - USTX 화면의 지갑 주문이 주문마다 두 곳의 값을 함께 보여 준다. 하나는 펀드(X Layer에 기록된 NAV, 수수료 없음), 다른 하나는 USTX/dUSD 풀(수수료 0.3%와 주문 자체의 가격 영향을 반영한 가격)이다. 더 많이 받는 쪽에 "Best price"를 표시하고 그쪽으로 보낸다. 방문자가 다른 쪽을 고를 수도 있다.
    - 풀 가격은 풀 컨트랙트와 같은 정수 계산식으로 구한다. 한 블록에서 풀 잔고와 풀에 대한 승인액을 함께 읽는다.
    - 풀에서 살 때는 데모 달러를 풀에 승인한 뒤 `buy`를, 팔 때는 USTX를 풀에 승인한 뒤 `sell`을 보낸다. 최소 수령액은 1%, 마감은 10분이며 마감은 체인 시각 기준이다. 체결은 풀의 `Bought`·`Sold` 이벤트로 읽는다.
    - 펀드가 NAV 기록을 기다리는 동안에도 풀로는 거래할 수 있다. 주문 종류 이름을 Buy/Redeem에서 Buy/Sell로 바꿨다.
    - `/developers` 시장 절에 경로 선택을 한 문장으로 적었다. 대출 시장의 OKX 탐색기 소스 검증 완료를 문서에 반영했다.
  - 배포 전
    - 활성 버전이 `b203515c`(`1e0465f`)임을 확인했다. 소스가 현재 `main`(`9281502`)을 포함한다.
    - 앱 검사 140개, 컨트랙트 검사 61개가 통과했고 lint 오류는 0이다(경고 10개는 main과 같다). 새 컨트랙트 검사는 앱이 만든 호출 데이터를, 고정 주소에 올린 풀 바이트코드에 그대로 보낸다. 앱의 풀 계산이 `quoteBuy`·`quoteSell`과 크기 4가지에서 일치했고, 승인·매도·매수가 실행되며 이벤트를 앱이 읽었다.
    - 로컬 빌드에서 테스트 지갑으로 실제 테스트넷 거래를 했다. 풀 승인을 0으로 되돌린 뒤 $20 풀 매수(승인→매수)와 전량 풀 매도(USTX 승인→매도)를 화면에서 끝까지 진행했고, 페이지 오류가 없었다. 로컬 빌드에서 axe 위반 0건, 가로 넘침이 없었다.
    - 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다.
  - 배포 후 운영 확인
    - 주요 12개 화면과 공개 API 4개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. `/developers`에 경로 선택 문장이 나온다.
    - 운영 화면에서 테스트 지갑으로 $20 풀 매수(승인·매수)와 전량 풀 매도(승인·매도)를 끝까지 진행했다. 거래 4건이 모두 성공했고(`0xce086ac2…92fb`, `0x1ab431ec…249c`, `0x44596cf4…7b27`, `0x955334c5…aa5b`), 페이지 오류가 없었다. $1,000 기본 주문에서는 펀드(10.003856 USTX)가 풀(8.343971 USTX)보다 나아 "Best price"로 표시됐다.
    - 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `b203515c`로 되돌릴 수 있다. 그 버전은 펀드로만 주문하고, 풀은 체인에서 그대로 동작한다.
- 2026-09-25 대출 시장 활성화와 대출 화면 배포: `0d989a7` → `de3e16c9-b6bd-4fe3-a025-7de5d3eb38a5`. 직전 버전은 `5d7eebbc`(`73d543f`)이다. 사용자가 "대출 활성화 승인"으로 활성화를 승인했다.
  - 체인
    - 관리자 키로 `unpause()`를 모의 실행한 뒤 보냈다(`npm run activate:lending`, 거래 `0x1acb998775cb55926d585611f9cc18171ab63015b86b62927828d237af6a873f`). 영수증 블록에서 `paused = false`를 확인했고, 배포 기록에 `activatedAt`·`unpauseTransaction`을 남겼다.
    - 관리자 지갑에는 dUSD가 없어서(다음 받기까지 21시간), 테스트 지갑이 첫 공급자로 $5,000를 넣었다(승인 `0x3070166a…e21d`, 공급 `0xaae154df99a842a354c832fbaefff5fdadaf5ae0538fdb3408b0dce1a5ea67af`). 공급 직후 시장 잔액은 $5,000, 대출 금리는 연 2%(사용률 0%)였다.
  - 바뀐 점
    - USTX 화면에 "Borrow against USTX" 절(`#borrow`, 메뉴에 Borrow)을 넣었다. 한 블록에서 시장 수치(빌릴 수 있는 금액, 대출·공급 총액, 사용률, 대출·공급 금리)와 지갑 포지션을 읽는다.
    - 포지션(담보 가치, 대출 한도, LTV, 청산 NAV)은 기록된 NAV로 컨트랙트와 같은 반올림으로 계산한다. 빌릴 수 있는 금액과 뺄 수 있는 담보에는 거래가 채굴될 때까지 붙는 이자를 위해 부채의 0.1%를 여유로 둔다. 실제 대출 바이트코드 검사에서 이 여유 없이는 최대 인출이 거절되는 것을 찾아 넣었다.
    - 동작은 USTX 입금, 대출, 상환(Max는 전액), USTX 인출, dUSD 공급, 공급 인출이다. 필요하면 승인하고, 모의 실행 뒤 OKX Wallet으로 보내며, 시장 이벤트로 결과를 읽는다.
    - 거래 뒤 그 블록 이후의 읽기가 올 때까지는 포지션에 "Updating"을 보이고 버튼을 잠근다. 포커스는 진행 단계와 완료 메시지로 옮긴다. 주문 패널과 대출 절은 서로 거래 블록을 알려 바로 다시 읽는다.
    - `/developers`에 "Use USTX as collateral"(설명, viem 예시, 탐색기 링크)를 넣었다. `/api/v1/ustx`가 `lending`(주소, 규칙, 탐색기 링크)을 준다.
  - 배포 전
    - 활성 버전이 `5d7eebbc`(`73d543f`)임을 확인했다. 소스가 현재 `main`(`4aa5d6b`)을 포함한다.
    - 앱 검사 146개(대출 6개 추가), 컨트랙트 검사 62개가 통과했고 lint 오류는 0이다. 새 컨트랙트 검사는 대출 시장의 코드와 생성자 상태를 고정 주소에 올려, 앱이 만든 호출 데이터로 한 바퀴를 돈다. 공급, 담보 입금, 대출, 이자 뒤 최대 대출, 부분 상환, 이자 뒤 최대 인출(대출 중 전액 인출은 거절), 전액 상환, 전액 인출, 이자 포함 공급 인출이다.
    - 로컬 빌드에서 테스트 지갑으로 실제 테스트넷 거래를 했다. 입금(승인·입금), $20 대출, 전액 상환(승인·상환), 전액 인출을 화면에서 진행했고, 매 단계 뒤 포지션이 새 값으로 바뀌고 포커스가 완료 메시지에 머무는 것을 확인했다. axe 위반 0건, 가로 넘침·페이지 오류가 없었다.
    - 깨끗한 빌드, 기존 D1과 `*/5` 일정, `--keep-vars`로 배포했다.
  - 배포 후 운영 확인
    - 주요 12개 화면과 공개 API 4개가 200을 반환하고, 이전 4개 경로가 새 화면으로 이동했다. USTX 화면에 대출 절이, `/developers`에 담보 절이, `/api/v1/ustx`에 `lending`이 나온다.
    - 운영 화면에서 테스트 지갑으로 2.003298 USTX 입금, $20 대출(LTV 10.00%, 청산 NAV $15.35), 전액 상환, 전액 인출을 끝까지 진행했다. 거래 6건이 모두 성공했고(`0xb6f5beea…a923`, `0x776f181e…7b9b`, `0x18d124de…47a2`, `0xe57a4ab4…dff9`, `0xca6905b1…b663`, `0xbfa24b4a…31cd`), 페이지 오류가 없었다.
    - 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건, 가로 넘침과 페이지 오류가 없었다.
    - 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 앱은 `5d7eebbc`로 되돌릴 수 있다. 대출 시장을 다시 멈추려면 관리자의 `pause()`가 필요하고, 사용자 승인을 받는다. 멈춰도 상환·인출·청산은 된다.
- 2026-09-25 포트폴리오 대출 표시 배포: `89181c8` → `5f124eee-407a-4a12-bbb6-483f8c5642aa`(직전 `de3e16c9`), 모바일 열 이름 수정 `3211c6f` → `15ae856c-d433-4f10-a3c1-deb825c170b8`(직전 `5f124eee`).
  - 바뀐 점
    - 포트폴리오의 지갑 USTX 절이 대출 시장도 읽는다. 담보로 넣은 USTX는 따로 한 행으로 보인다. 그 행에는 가치, 대출 한도, 대출 잔액과 LTV, 대출 절로 가는 Manage 링크가 있다. 공급한 dUSD가 있으면 아래에 공급액과 공급 금리를 적는다.
    - 종목별 들여다보기는 지갑 USTX와 담보 USTX를 합쳐 계산한다. 담보도 상환하면 돌아오는 그 지갑의 USTX다. 대출 시장 읽기가 실패해도 지갑 USTX는 그대로 보인다.
    - 연 0.01% 미만의 금리는 0.00% 대신 "under 0.01%"로 적는다. 지갑 행의 Redeem 링크를 주문 패널과 같게 Sell로 바꿨다.
  - 실제 예시
    - 테스트 지갑이 1 USTX를 담보로 넣고 $10를 빌린 상태로 남겨 두었다(승인 `0x237dfbc3…e4e7`, 입금 `0xe7545da1…c311`, 대출 `0x4f761f4dccfd65054c68c6aa961dd5e91f2800e7cadfe6179664dbd3cb06f018`). 그래서 대출 절에 대출 총액 $10와 0보다 큰 공급 금리가 보인다. 청산까지는 NAV가 84.6% 넘게 떨어져야 한다.
  - 검사
    - 앱 검사 146개와 컨트랙트 검사 62개가 통과했고 lint 오류는 0이다.
    - 운영에서 테스트 지갑으로 포트폴리오를 열었다. 지갑 1.003298 USTX와 담보 1.000000 USTX(대출 $10.00, 가치의 10.00%)가 나왔고, 들여다보기가 합계 기준으로 계산됐다. 데스크톱·모바일 가로 넘침과 페이지 오류가 없었다.
    - 주요 12개 화면과 공개 API 4개 200, 이전 4개 경로 307을 확인했다. 데스크톱(1440px)과 모바일(390px), 지갑 있음·없음으로 7개 화면 모두 axe 위반 0건이었다. 샘플 확인이며 연속 무장애 증명이 아니다.
  - 필요 시 `de3e16c9`로 되돌릴 수 있다.
